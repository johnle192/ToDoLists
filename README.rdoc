== README

This is a to do list created with Rails in preparation for an apprenticeship interview. This app can be run locally with the following steps:

* <tt>rails server</tt>
* Navigate to <tt>https://localhost:3000/users</tt>
